

Key Features:

> Upload via FTP
> text file log
> MySQL database logs
> real-time server logging
> all fields are required - cannot be bypassed unless HTML-modified via inspect
> SQL logs can be viewed via Web GUI: bleepinghost.online/FTP/logs

New:
> SESSION cookies for username and password
: once the user has input the correct username and pass, credentials are retained upon returning back to the uploader page
