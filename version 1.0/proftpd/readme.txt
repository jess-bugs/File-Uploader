


In able to avoid FTP server errors, you should comment out the "DefaultRoot ~" in proftpd.conf file:

In this folder, you will see proftpd.conf file which is configured to work without errors. Replace your current proftpd.conf file with this one if you encounter any errors.

~good luck.